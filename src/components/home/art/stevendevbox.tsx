import type { ArtProps } from './types';

/** Pendiente: el arte de stevendevbox. */
export default function Art(_: ArtProps) {
  return <div className="art art-stevendevbox" aria-hidden="true" />;
}
