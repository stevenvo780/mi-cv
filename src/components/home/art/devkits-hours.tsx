import type { ArtProps } from './types';

/** Pendiente: el arte de devkits-hours. */
export default function Art(_: ArtProps) {
  return <div className="art art-devkits-hours" aria-hidden="true" />;
}
