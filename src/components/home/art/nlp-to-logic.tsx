import type { ArtProps } from './types';

/** Pendiente: el arte de nlp-to-logic. */
export default function Art(_: ArtProps) {
  return <div className="art art-nlp-to-logic" aria-hidden="true" />;
}
