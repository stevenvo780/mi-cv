import type { ArtProps } from './types';

/** Pendiente: el arte de estructuras-preontologicas. */
export default function Art(_: ArtProps) {
  return <div className="art art-estructuras-preontologicas" aria-hidden="true" />;
}
