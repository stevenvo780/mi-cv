import type { ArtProps } from './types';

/** Pendiente: el arte de umbral-atlas. */
export default function Art(_: ArtProps) {
  return <div className="art art-umbral-atlas" aria-hidden="true" />;
}
